window.generatedGuides = {};
const R={cpp:[['cppreference','https://en.cppreference.com/w/'],['C++ Core Guidelines','https://isocpp.github.io/CppCoreGuidelines/CppCoreGuidelines']],os:[['Linux man-pages','https://man7.org/linux/man-pages/'],['The Linux Programming Interface','https://man7.org/tlpi/']],net:[['Beej’s Guide to Network Programming','https://beej.us/guide/bgnet/'],['Linux socket manual','https://man7.org/linux/man-pages/man2/socket.2.html']]};
function guide(n,track,title,why,code,plan,refs){window.generatedGuides[n]={track,title,lede:why,outcome:`you can apply ${title.toLowerCase()} in a small C++ backend exercise, explain its trade-offs, and verify the behavior with a focused test.`,sections:[['Why this matters',`<p>${why}</p><p>Do not treat this as vocabulary to memorize. Connect it to resource ownership, request handling, latency, failure handling, or deployment—the concerns a backend service actually has.</p>`],['Build the smallest correct example',`<p>Start in a throwaway executable, compile with warnings enabled, and make the observable behavior explicit.</p><pre><code>${code}</code></pre><p>Read every return value or state transition. Then deliberately test both the normal path and one failure or boundary case.</p>`],['Turn it into practice',`<p>${plan}</p><p>Keep the interface narrow, write down the invariant it relies on, and only then fold it into a larger project. Small experiments are where you discover what the abstraction promises and what it does not.</p>`]],checklist:['You can explain the concept without using its name as the definition.','You compiled and ran the example or an equivalent experiment.','You tested one boundary, error, or shutdown path.','You recorded one rule you will use in the capstone project.'],refs};}
const C=R.cpp,O=R.os,N=R.net;
guide(16,'Modern C++','Learn copy constructors','Copy construction creates a distinct object from an existing value. Value members such as string and vector already copy correctly, so the Rule of Zero is the default.','struct Config{std::string host;int port;}; Config a{"localhost",8080}; Config b{a}; b.port=9090;','Copy a type containing vector and prove that changing the copy does not mutate the original. Then explain why a raw owning pointer would make compiler-generated copying unsafe.',C);
guide(17,'Modern C++','Learn move semantics','Moving transfers resources from an object that is about to lose its useful value, avoiding expensive copies of buffers and handles. A moved-from object remains valid but its value is unspecified.','std::vector&lt;int&gt; payload=load(); std::vector&lt;int&gt; queued=std::move(payload);','Instrument a type or inspect vector capacity before and after a move. Never read business data from the moved-from object; assign a new value or destroy it.',C);
guide(18,'Modern C++','Learn Rule of Five','A type managing a raw resource may need copy constructor, copy assignment, move constructor, move assignment and destructor. Better yet, replace raw ownership with standard RAII members and need none.','class File{std::unique_ptr&lt;State&gt; state_; public: File()=default; File(const File&amp;)=delete; File(File&amp;&amp;) noexcept=default;};','Make a move-only resource wrapper, verify copying fails at compile time, and verify moving transfers responsibility for cleanup.',C);
guide(19,'Modern C++','Learn unique_ptr','unique_ptr expresses exclusive ownership and destroys its object automatically. It is the right default for dynamically allocated polymorphic objects and resources with one owner.','auto socket=std::make_unique&lt;Socket&gt;(fd); socket-&gt;send(data);','Replace one raw new/delete pair with make_unique. Pass a reference for borrowing; move the unique_ptr only when ownership genuinely transfers.',C);
guide(20,'Modern C++','Learn shared_ptr','shared_ptr uses reference counting for genuinely shared lifetime. It solves lifetime, not synchronization, and cycles prevent destruction.','auto config=std::make_shared&lt;Config&gt;(); worker(config);','Draw the ownership graph for a shared configuration object. Prefer a clear single owner when possible; do not use shared_ptr merely to avoid deciding ownership.',C);
guide(21,'Modern C++','Learn weak_ptr','weak_ptr observes a shared object without extending its lifetime. It breaks ownership cycles and lets caches or callbacks check whether an owner remains alive.','if(auto session=weak.lock()){ session-&gt;send(reply); }','Create two objects that would cycle through shared_ptr, change one edge to weak_ptr, and observe both destructors run after external owners leave.',C);
guide(22,'Modern C++','Learn STL containers','Containers encode common storage, ownership and complexity trade-offs. Choose by operations: contiguous iteration, stable references, fast lookup, queueing or ordered traversal.','std::vector&lt;Request&gt; batch; std::unordered_map&lt;std::string,int&gt; counts;','Implement request-status counting with vector and unordered_map. Measure or reason about lookup, insertion and iteration rather than selecting by habit.',C);
guide(23,'Modern C++','Learn iterators','Iterators are the uniform way to traverse containers and connect them to algorithms. Iterator invalidation rules matter whenever a container mutates.','for(auto it=items.begin(); it!=items.end(); ++it) process(*it);','Erase selected vector elements with the erase-remove idiom, then read the container’s invalidation rules before storing any iterator across mutation.',C);
guide(24,'Modern C++','Learn STL algorithms','Algorithms make intent visible and reduce hand-written loop bugs. They operate on iterator ranges and predicates, separating traversal from business rules.','auto bad=std::find_if(v.begin(),v.end(),[](int x){return x&gt;=500;});','Replace three loops with find_if, transform and count_if. Check the returned iterator before dereference and use ranges as half-open intervals.',C);
guide(25,'Modern C++','Learn lambdas','Lambdas package local behavior for algorithms, callbacks and asynchronous work. Capture choices control both lifetime and mutation.','auto isError=[](int code){return code&gt;=500;};','Write value and reference captures, then move a lambda into an async task. Ensure every captured reference outlives execution; default to explicit captures.',C);
guide(26,'Modern C++','Learn templates','Templates generate type-specific code while preserving compile-time checking. They are valuable for reusable algorithms, but error messages and interfaces demand constraints and restraint.','template&lt;class T&gt; T clamp(T x,T lo,T hi){return std::min(std::max(x,lo),hi);}','Write a small generic function, test it with two types, and state the operations its type parameter must support. Prefer concepts when your compiler standard allows them.',C);
guide(27,'Modern C++','Learn optional','optional represents an expected absence without inventing sentinel values or throwing. The caller must explicitly decide what absence means.','std::optional&lt;User&gt; find_user(Id id); if(auto u=find_user(id)) serve(*u);','Use optional for a lookup that may miss. Keep transport/database errors separate: absence is not the same as failure.',C);
guide(28,'Modern C++','Learn variant','variant holds exactly one member of a closed set of alternatives. It models a result whose valid cases are known, with visitors forcing case handling.','using Reply=std::variant&lt;Ok,NotFound,Invalid&gt;; std::visit(handle,reply);','Model parser outcomes as a variant and write a visitor for every alternative. Add one alternative and let the compiler show unhandled code paths.',C);
guide(29,'Modern C++','Learn exceptions','Exceptions carry exceptional failures to a boundary that can add context, log and choose recovery. RAII ensures acquired resources release while stack unwinding occurs.','try{run();}catch(const std::exception&amp; e){std::cerr&lt;&lt;e.what(); return 1;}','Throw a standard exception for invalid configuration and catch it by const reference at main. Do not catch-and-ignore; use optional or a result type for routine absence.',C);
guide(30,'Modern C++','Build a modern C++ CLI project','Integration reveals whether individual language features form a coherent design. A small CLI gives you a complete build, input, error and test loop before OS work.','cmake -S . -B build &amp;&amp; cmake --build build &amp;&amp; ./build/analyze sample.log','Build a request-log analyzer using CMake, ifstream, from_chars, vector, unordered_map, optional and find_if. Document build/run/test commands and run sanitizers.',C);
guide(31,'Operating systems','Understand processes','A process has a PID, virtual address space, credentials, scheduling state and descriptors. Service managers observe this lifecycle and restart or report failures.','sleep 60 &amp; pid=$!; ps -o pid,ppid,stat,command -p $pid; ls -l /proc/$pid/fd','Inspect a running process and its parent. Create a child, wait for it, and explain why an unreaped exited child becomes a zombie.',O);
guide(32,'Operating systems','Understand threads','Threads share process memory and descriptors but have independent stacks and registers. This enables efficient sharing and makes unsynchronized mutation unsafe.','process: accept-thread + workers + log-thread','List which resources are shared and private. Compare a CPU-bound workload with an I/O-bound workload and measure whether extra threads help.',O);
guide(33,'Operating systems','Understand virtual memory','Virtual memory isolates processes and maps virtual pages to physical memory on demand. A page fault can be normal lazy allocation or an invalid access.','std::vector&lt;char&gt; b(64*1024*1024); for(auto&amp; x:b)x=0;','Inspect /proc/PID/maps and compare virtual size with RSS. Explain copy-on-write and why a pointer cannot be used in another process.',O);
guide(34,'Operating systems','Understand stack and heap memory','Automatic storage gives deterministic scope lifetime; dynamic storage needs an owner. C++ RAII makes heap resources safe when value lifetime must extend.','void f(){Request r; auto p=std::make_unique&lt;Cache&gt;();}','Demonstrate a dangling reference bug under AddressSanitizer, then replace raw allocation with vector or unique_ptr and explain the ownership graph.',C);
guide(35,'Operating systems','Learn system calls','System calls request kernel work and report success or failure precisely. Read their contracts rather than assuming libc wrappers always behave like ordinary functions.','int fd=open("config",O_RDONLY); if(fd==-1) throw std::system_error(errno,std::generic_category());','Read man 2 open and man 3 printf. Wrap one raw syscall result in an RAII class that preserves errno context immediately on error.',O);
guide(36,'Operating systems','Learn file descriptors','A descriptor is a per-process integer handle for files, sockets, pipes and terminals. Every owned descriptor must close exactly once.','int fd=open(path,O_RDONLY); // close(fd) via RAII','Write a move-only UniqueFd wrapper and inspect /proc/PID/fd or lsof while it is open. Set close-on-exec where inheritance is not intended.',O);
guide(37,'Operating systems','Use open read and write','I/O transfers bytes, possibly partially. Correct code loops over interrupted calls and tracks progress until EOF or the full message is handled.','while(sent&lt;msg.size()){auto n=write(fd,msg.data()+sent,msg.size()-sent); sent+=size_t(n);}','Copy a file with a small buffer. Handle zero as EOF, EINTR as retry, and partial writes; then explain why a network protocol needs framing.',O);
guide(38,'Operating systems','Use pipes','A pipe is a local unidirectional byte stream. EOF reaches the reader only after every write end closes, including inherited copies after fork.','int p[2]; pipe(p); write(p[1],"ok",2); read(p[0],buf,2);','Connect a parent producer to a child consumer. Close unused endpoints in both processes and show the reader blocking when an accidental writer remains open.',O);
guide(39,'Operating systems','Learn fork','fork creates a child path with a new PID and copy-on-write memory. In a multithreaded parent it is restricted because vanished threads may have held locks.','pid_t pid=fork(); if(pid==0)_exit(0); waitpid(pid,nullptr,0);','Branch correctly for failure, child and parent. Test copy-on-write conceptually, reap the child, and explain why fork plus exec is safer than doing complex work in the child.',O);
guide(40,'Operating systems','Learn exec','exec replaces a process image while retaining PID and selected descriptors. Success does not return, so the child must report failure and _exit.','char* a[]={const_cast&lt;char*&gt;("/bin/echo"),nullptr}; execv(a[0],a); _exit(127);','Launch a known binary with fork and execv, not a shell. Verify close-on-exec behavior and pass arguments as argv data.',O);
guide(41,'Operating systems','Understand signals','Signals asynchronously notify a process about interruption, termination or child state. Handlers must do almost nothing; normal control flow should own cleanup.','std::atomic_bool stop=false; void onterm(int){stop.store(true);} ','Install SIGTERM handling with a stop flag. Make the main loop stop accepting work and join workers; do not log or lock from the handler.',O);
guide(42,'Concurrency','Start std thread','std::thread creates concurrent execution but does not manage data safety or shutdown automatically. Every owned thread needs a join policy.','std::thread t(work,42); t.join();','Pass copied arguments, then compare std::ref behavior. Use jthread where available and explain why detached threads are hazardous for request-owned data.',C);
guide(43,'Concurrency','Recognize race conditions','Concurrent unsynchronized access with a write is a data race and undefined behavior in C++. The strongest fix is often eliminating shared mutable state.','int n=0; std::thread a([&]{++n;}),b([&]{++n;}); a.join();b.join();','Run the example under ThreadSanitizer. Redesign using per-thread results, a mutex, or an atomic according to the invariant.',C);
guide(44,'Concurrency','Use mutexes','A mutex protects an invariant across one or more fields. RAII locking releases it during all exits, including exceptions.','std::lock_guard lock(m); jobs.push_back(job);','Protect a queue and its related state with one mutex. Copy data out before logging or calling external code, so slow work occurs after unlock.',C);
guide(45,'Concurrency','Use scoped locking','Multiple locks need coordinated acquisition or a fixed global order to avoid deadlocks. scoped_lock acquires related mutexes safely.','std::scoped_lock lock(accountA.m,accountB.m);','Implement a two-account transfer and state the invariant that holds while both locks are taken. Compare lock_guard, unique_lock and scoped_lock.',C);
guide(46,'Concurrency','Use condition variables','Condition variables sleep until a predicate over mutex-protected state changes. The predicate handles spurious wakeups and races between notification and reacquisition.','cv.wait(lock,[&]{return stopping||!q.empty();});','Build producer/consumer logic. Change state under the lock, notify after, and use notify_all when setting shutdown so every worker exits.',C);
guide(47,'Concurrency','Use atomics','Atomics make individual operations indivisible and can enforce memory ordering, but they do not automatically make multi-field invariants safe.','std::atomic_uint64_t count{0}; count.fetch_add(1,std::memory_order_relaxed);','Use an atomic counter and stop flag. Explain why relaxed is enough for a metric but a mutex is safer for a queue plus its size.',C);
guide(48,'Concurrency','Prevent deadlocks','Deadlocks form cycles of waiting. Prevention comes from lock ordering, coordinated locking, less nesting and avoiding callbacks while holding locks.','std::scoped_lock lock(left.m,right.m);','Draw a two-thread wait-for cycle, then remove it with a stable order or message passing. Capture thread stacks when diagnosing a deliberate stall.',C);
guide(49,'Concurrency','Build a thread-safe queue','A blocking queue transfers ownership between producers and consumers. Its container and close state share a mutex; a predicate controls consumer sleep.','cv.wait(lock,[&]{return closed||!q.empty();}); if(q.empty()) return {};','Implement push, pop and close. Test multiple consumers, close while waiting, and draining work that arrived before close.',C);
guide(50,'Concurrency','Build a thread pool','A thread pool owns a bounded number of workers consuming queued tasks. Its value is controlled concurrency and predictable shutdown, not simply parallelism.','while(auto task=queue.pop()) (*task)();','Build workers around your queue, catch task exceptions at the boundary, and close/join deterministically. Add packaged_task and future only after void tasks work.',C);
guide(51,'Networking','Learn the TCP IP model','Application protocols sit over TCP or UDP, IP routing and local link delivery. Layering makes a network failure diagnosable instead of mysterious.','HTTP request → TCP bytes → IP packet → link frame','Trace one HTTP request by layer. Explain that TCP supplies ordered bytes, while HTTP—not TCP—defines message boundaries.',N);
guide(52,'Networking','Understand IP addresses','IP addresses identify interfaces for routing; prefixes identify networks. IPv4 and IPv6 have different sizes but share the CIDR prefix concept.','192.168.1.20/24; 2001:db8::1/64','Identify loopback, private IPv4 and public addresses. Bind a server to 127.0.0.1, then explain why 0.0.0.0 changes reachability.',N);
guide(53,'Networking','Understand ports','Ports identify transport endpoints on a host. A listener owns an address-port-protocol combination; clients receive ephemeral local ports.','ss -ltnp; lsof -i :8080','Run a local service on 8080 and inspect its listener. Explain why a reverse proxy may expose 443 while the service listens only on a private port.',N);
guide(54,'Networking','Understand DNS','DNS resolves names into records that can be cached, load-balanced and changed. Name resolution is a separate stage from connecting to a service.','dig example.com A; dig example.com AAAA; getaddrinfo(host,service,&amp;hints,&amp;res);','Inspect A, AAAA and CNAME records. Use getaddrinfo rather than hard-coding IPv4, and log resolution failures separately from connection failures.',N);
guide(55,'Networking','Understand TCP connections','TCP provides a reliable ordered full-duplex byte stream. A server listens, accepts per-client sockets, frames messages, limits resources and handles closure.','socket → bind → listen → accept → read/write → shutdown/close','Build a blocking loopback server skeleton. Implement a length or delimiter framing rule, cap message size, set timeouts, and handle read==0 as peer closure.',N);
